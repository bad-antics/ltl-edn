// ltl-edn/main.ltl
//
// Clojure EDN (extensible data notation) reader.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-edn v0.1.0")
    io::println("Clojure EDN (extensible data notation) reader.")
    0
}
