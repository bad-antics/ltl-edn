// examples/demo.ltl — runnable demo for ltl_edn

import std::io
use ltl_edn as lib

fn main() {
    io::println("ltl_edn demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}